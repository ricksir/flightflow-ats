{
  "name": "flightflow-ats",
  "version": "0.2.1-dev",
  "private": true,
  "description": "FlightFlow ATS — baseline e testes de regressão do núcleo temporal/espacial e navegação de UI",
  "scripts": {
    "start": "node tools/dev-server.mjs",
    "test": "node --test tests/*.test.js",
    "test:ui": "playwright test",
    "audit": "python3 tools/audit_static.py index.html",
    "inventory": "python3 tools/function_inventory.py index.html --check",
    "check": "npm run audit && npm run inventory && npm test && npm run test:ui"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  },
  "engines": {
    "node": ">=22"
  }
}
