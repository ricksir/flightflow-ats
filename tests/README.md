# Testes

Nesta fase existe auditoria estática e checklist manual. A próxima etapa é adicionar testes automatizados de comportamento da timeline, rota e histórico antes de extrair módulos do HTML monolítico.

Execute a verificação estática:

```bash
python tools/audit_static.py index.html
```
